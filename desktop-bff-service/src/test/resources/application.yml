spring:
  cloud:
    config:
      enabled: false
  config:
    import: optional:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://issuer.example.com/

eureka:
  client:
    enabled: false

auth0:
  audience: https://api.coboxsv.dev
